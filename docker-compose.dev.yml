# Explicit local override; all debugging ports are loopback-only.
services:
  db:
    ports: ['127.0.0.1:5432:5432']
  minio:
    ports: ['127.0.0.1:9000:9000']
  backend:
    ports: ['127.0.0.1:8000:8000']
  frontend:
    ports: ['127.0.0.1:3000:8080']
